const opened=JSON.parse(localStorage.getItem('bm_opened')||'[]');fetch('ideas.json').then(r=>r.json()).then(ideas=>{const g=grid;const f=()=>{count.textContent=`${opened.length} / ${ideas.length} opened`;bar.style.width=`${opened.length/ideas.length*100}%`};f();ideas.forEach((it,i)=>{const c=document.createElement('div');c.className='card'+(opened.includes(i)?' open':'');c.innerHTML=`${String(i+1).padStart(2,'0')}${it.nsfw?'<span class="badge">🔥</span>':''}`;c.onclick=()=>{day.textContent=`Day ${i+1}`;title.textContent=it.title;text.textContent=it.text;if(it.link){linkBtn.href=it.link;linkBtn.style.display='inline-block'}else linkBtn.style.display='none';modal.showModal();if(!opened.includes(i)){opened.push(i);localStorage.setItem('bm_opened',JSON.stringify(opened));c.classList.add('open');f();}};g.appendChild(c)});closeBtn.onclick=()=>modal.close();modal.addEventListener('click',e=>{const r=modal.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)modal.close()})});